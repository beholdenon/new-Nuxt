export default defineAppConfig({
  ui: {
    table: {
        wrapper: 'relative overflow-x-auto',
        base: ' table-fixed',
        td: {
            base: 'whitespace-normal align-top',
        },
    }
  }
})